<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal de la aplicación -->
    <style name="miEstilo" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/azul_claro</item>
        <!-- Estilo de los textos -->
        <item name="android:textColor">@color/color_Textos</item>
        <item name="android:fontFamily">roboto</item>
    </style>

    <style name="EstiloBoton" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/azul_oscuro</item>

    </style>

</resources>